//Create an array which consists of few names of your friends and 
// use a for loop to iterate through the array and print out the contents.

// const friends = (array) => {
    const array = ['Matthew', 'Mark', 'Luke', 'John']
    for (let i = 0; i < array.length; i++) {

        console.log(array[i]);

    }

    


// console.log(array);